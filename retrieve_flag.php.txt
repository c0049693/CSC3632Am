<?php
// Set the URL and token
$url = 'http://10.0.0.6/ctf_deploy/opensource/4YtIa0G6/index.php';
$token = 'CXEYBG1K';

// Create the URL with the token
$request_url = $url . '?XYZ=' . $token;

// Make an HTTP GET request to the URL
$response = file_get_contents($request_url);

// Output the response
echo $response;
?>
